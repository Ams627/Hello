using System.CodeDom;


//this is a test

if(1==1) Console.WriteLine("Hello, World!");

var str = "Hello";
ReadOnlySpan<char> slice = str[1..2];

public enum C {
    Dog=1,
    Cat=2
}

